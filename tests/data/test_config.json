{
  "input-files": ["tests/data/sample.xlsx"],
  "output-dir": "tests/data/output", "inputs": ["tests/data/table1.xlsx"],
  "output_dir": "tests/data/output",
  "prefix": "json",
  "keep-empty": true,
  "log-level": "INFO",
  "containers": {
    "json.表1": {
      "range": "表1.range",
      "direction": "row",
      "items": ["列A", "列B", "列C"],
      "labels": ["列A", "列B", "列C"]
    }
}
